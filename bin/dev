#!/usr/bin/env sh
# Root orchestrator — runs the Rails Procfile.dev from backend/.
# Rails bin/dev expects to be invoked from backend/, which is where its
# Procfile.dev (with the engine sidecar entry) lives.
cd "$(dirname "$0")/../backend" && exec ./bin/dev "$@"
